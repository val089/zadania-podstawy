// ## Zadanie 1
// Zamień na funkcje strzałkową poniższe funkcje:

const sum = (a, b) => console.log(result);

const myPow = (a, b) => Math.pow(a, b);

const hr = () => console.log("-------");

const bigWords = tab => tab.map(el => el.toUpperCase());

const sumTab = tab => tab.reduce((a, b) => a + b);

const test = _ => "abecadło z pieca spadło";

(() => {
    console.log("Prawie koniec");
});

const findFirst = (arr, name) => arr.find(el => el === name);